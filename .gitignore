# Local-only directories
archive/
code/

# All generated/downloaded results live under results/ — local ci-test.sh
# output at its root, cloud downloads in results/aws/ and results/azure/,
# and the raw snapshot of the deleted S3 bucket in results/s3-final-backup/
# (evidence is archived in record-results/; these are working copies).
results/

# Downloaded paper PDFs (copyright — keep local only)
papers/literature/

# macOS
.DS_Store
